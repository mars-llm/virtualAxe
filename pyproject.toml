[project]
name = "virtualaxe"
version = "0.1.0"
description = "virtual Bitaxe Gamma release and validation tooling"
requires-python = ">=3.12"
dependencies = [
  "pytest==9.0.3",
  "requests==2.33.1",
  "rich==15.0.0",
  "textual==8.2.4",
]

[tool.uv]
package = false
